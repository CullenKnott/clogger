<div class="row">
    <div class="col-auto">
        <h2>Logged in as {{name}}</h2>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-lg-6 col-md-12">
            <form class="form blog-form">
                <div class="form-group">
                    <label for="title-blog">title:</label>
                    <input type="text" class="form-input" id="title-blog">
                </div>
                <div class="form-group">
                    <label for="text-blog">text:</label>
                    <textarea name="text-blog" id="text-blog" cols="30" rows="10"></textarea>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Post!</button>
                </div>
            </form>
        </div>
    </div>

    {{#if blogposts.length}}
    <div class="col-lg-2 col-md-12">
        <h3>Your Blogposts:</h3>

        {{#each blogposts as |blogpost|}}
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <h4><a href="/blogpost/{{blogpost.id}}">{{blogpost.title}}</a></h4>
                </div>
            </div>
            <div class="col-md-4">
                <button class="btn btn-sm btn-danger" data-id="{{blogpost.id}}"></button>
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}
</div>

<script src="./js/profile.js"></script>