<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clogger</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="">
                    <h1>
                        <a href="/">Clogger: Code Blogger</a>
                    </h1>
                    <nav class="navbar navbar-expand-lg">
                        <div class="container-fluid">
                            <div class="collapse navbar-collapse" id="navBarNav">
                                <ul class="navbar-nav">
                                    <!-- renders links based on login/logout session-->
                                    {{#if logged_in}}
                                    <li class="nav-item">
                                        <a href="/profile" class="btn btn-sm btn-outline-primary">profile</a>
                                    </li>
                                    <li class="nav-item">
                                        <button class="btn btn-outline-danger btn-sm">logout</button>
                                    </li>
                                    {{else}}
                                    <li>
                                        <a href="/login" class="btn btn-outline-success btn-sm">login</a>
                                    </li>
                                    {{/if}}
                                </ul>
                            </div>
                        </div>
                    </nav>
                </div>
            </div>
        </div>
        <main class="container-fluid">
            {{!-- render the other pages withing the main page --}}
            {{{ body }}}
        </main>
        <footer>

        </footer>
    </div>

    {{#if logged_in}}
    <script src="/public/js/logout.js"></script>
    {{/if}}
</body>
</html>